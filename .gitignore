# Environment / secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/

# Virtualenvs
.venv/
venv/

# uv — the single workspace lock IS committed; nothing to ignore there.

# Runtime data directories produced by the services
grabette-data/
casquette-data/
gripette-data/

# NOTE: generated protobuf stubs (*_pb2.py / *_pb2_grpc.py / *.pyi) are
# committed in this project (gripette, openarm sim import them directly),
# so they are intentionally NOT ignored.

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
*~

# evaluate.py --debug headless frame dumps
eval_debug_frames/

# The Orbbec SDK writes Log/OrbbecSDK.log.txt into the CWD of whatever loads it.
# On a deployed Pi the daemon's WorkingDirectory IS this repo, so a checkout
# running depth_camera=gemini305 grows an untracked Log/ (1.8 MB after a day).
Log/

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
